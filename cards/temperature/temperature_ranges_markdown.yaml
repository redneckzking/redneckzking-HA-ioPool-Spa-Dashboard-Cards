type: markdown
entity_id:
  - sensor.iopool_saluspa_bahama_temperature
content: >
  {% set v = states('sensor.iopool_saluspa_bahama_temperature') | float(none) %}

  {% if v is number %}
    {% if v < 59 %}
      {% set status = '🔴 LOW' %}
    {% elif v < 68.9 %}
      {% set status = '🟡 COOL' %}
    {% elif v <= 84.2 %}
      {% set status = '🟢 TARGET' %}
    {% elif v <= 89.6 %}
      {% set status = '🟡 WARM' %}
    {% else %}
      {% set status = '🔴 HIGH' %}
    {% endif %}
  {% else %}
    {% set status = '⚪ NO DATA' %}
  {% endif %}


  **Temperature Ranges** · **{{ (v | round(1)) if v is number else '—' }}°F** ·
  {{ status }}


  - 🔴 **< 59°F** = Low

  - 🟡 **59–68.9°F** = Cool

  - 🟢 **68.9–84.2°F** = Target

  - 🟡 **84.3–89.6°F** = Warm

  - 🔴 **> 89.6°F** = High
card_mod:
  style: |
    ha-card {
      border-radius: 18px;
      padding: 4px 10px 6px 10px;
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(20,20,24,0.55);
      box-shadow: 0 6px 18px rgba(0,0,0,0.18);
    }
    ha-markdown {
      font-size: 11px;
      line-height: 1.2;
    }
    ha-markdown p {
      margin: 0 0 4px 0 !important;
    }
    ha-markdown ul {
      margin: 0 !important;
      padding-left: 16px !important;
    }
    ha-markdown li {
      margin: 0 !important;
      line-height: 1.15 !important;
    }

